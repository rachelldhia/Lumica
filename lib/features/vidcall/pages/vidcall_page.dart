import 'package:flutter/material.dart';

class VidcallPage extends StatelessWidget {
  const VidcallPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Vidcall Page'),
      ),
    );
  }
}
